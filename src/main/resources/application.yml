spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hanghae_backend
    username: root
    password: park0719!@#
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
    properties:
      hibernate:
        type: trace
